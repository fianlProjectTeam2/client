import React from "react";

function Main() {
  const appStyle = {
    display: "flex",
    flexDirection: "column",
    height: "100vh",
  };

  const layoutStyle = {
    display: "flex",
    flex: 1,
  };

  return (
    <div style={appStyle}>
      <div style={layoutStyle}>

      </div>
    </div>
  );
}

export default Main;
